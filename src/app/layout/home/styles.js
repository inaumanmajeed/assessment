import { styled } from "@mui/material/styles";

const DasboardLayoutContainer = styled("div")(({ theme }) => ({
  width: "100%",
  height: "100dvh",
  display: "flex",
}));

export { DasboardLayoutContainer };
